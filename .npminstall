#/usr/bin/bash

sayhead "Installing npm packages"

# Yeoman
saybody "Installing yeoman"
sudo npm install yo -g;

# Grunt
saybody "Installing grunt-cli"
sudo npm install grunt-cli -g;

# Bower
saybody "Installing bower"
sudo npm install bower -g;
